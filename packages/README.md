# packages/

Código compartilhado entre `apps/web` e `apps/api` (opcional).

Uso previsto: tipos TypeScript comuns, schemas de validação (ex.: Zod) e
constantes de domínio (status de imóvel, papéis de corretor etc.).

> Pode ser adicionado quando houver duplicação real entre frontend e backend.
> Não é obrigatório na Sprint 1.
